import React from 'react';

import './button.css'

const Button = (props) => {
  return ( 
    <button 
      autoFocus="true"
      className="button"
    >Next</button>
   );
}
 
export default Button;